# TemplateApp

Svelte 5 SPA generated with [Templetry](https://github.com/Templetry): Vite + TypeScript, runes, optional Vitest.

```sh
npm install
npm run dev
npm run build     # svelte-check + vite build
npm test          # vitest feature
```
