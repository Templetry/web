# Committed on purpose: these are defaults, never secrets. Only VITE_-prefixed
# values reach the browser, so nothing here may be sensitive.
VITE_ENVIRONMENT=development
VITE_API_BASE_URL=http://localhost:8080
VITE_VERBOSE_ERRORS=true
