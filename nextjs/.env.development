# Committed on purpose: these are defaults, never secrets. NEXT_PUBLIC_ values
# are inlined into the browser bundle, so nothing here may be sensitive.
NEXT_PUBLIC_ENVIRONMENT=development
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
NEXT_PUBLIC_VERBOSE_ERRORS=true
