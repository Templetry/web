# TemplateApp

Next.js app generated with [Templetry](https://github.com/Templetry): App Router, TypeScript, optional route handler API and Vitest.

```sh
npm install
npm run dev        # :3000
npm run build && npm start
npm test           # vitest feature
```

With the `api_route` feature: `GET /api/hello?name=you`.
